diff a/sin/tests/Pest.php b/sin/tests/Pest.php	(rejected hunks)
@@ -0,0 +1,28 @@
+<?php
+
+use App\Admin\Models\User;
+use Illuminate\Support\Facades\Artisan;
+use Tests\CreatesApplication;
+
+use function Pest\Laravel\actingAs;
+
+use Illuminate\Foundation\Testing\TestCase;
+
+uses(TestCase::class, CreatesApplication::class)
+    ->in(__DIR__);
+
+uses()->beforeEach(function () {
+    Artisan::call('migrate:fresh', ['--database' => 'db_biblioteca', '--path' => "database/migrations/biblioteca"]);
+})->in('Biblioteca');
+
+
+function login(User $user = null): User
+{
+    if (is_null($user)) {
+        $user = User::where('username', "=", 'Admin')->first();
+    }
+
+    actingAs($user);
+
+    return $user;
+}
